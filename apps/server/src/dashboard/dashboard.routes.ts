import type{FastifyInstance}from"fastify";import{DashboardRepository}from"./dashboard.repository.js";export async function dashboardRoutes(app:FastifyInstance,repository:DashboardRepository):Promise<void>{app.get("/api/dashboard",async request=>{const query=request.query as{courseId?:string;sessionId?:string};return{data:repository.get(query.courseId,query.sessionId),requestId:request.id};});}
