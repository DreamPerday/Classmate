import type{FastifyInstance}from"fastify";import{eventBus}from"./event-bus.js";import{config}from"./config.js";
const origins=new Set([`http://127.0.0.1:${config.port}`,"http://127.0.0.1:5173","http://localhost:5173","null"]);
export async function sseRoutes(app:FastifyInstance):Promise<void>{app.get("/api/events",async(request,reply)=>{const origin=request.headers.origin;const headers:Record<string,string>={"Content-Type":"text/event-stream","Cache-Control":"no-cache, no-transform","Connection":"keep-alive","X-Accel-Buffering":"no","Vary":"Origin"};if(origin&&origins.has(origin))headers["Access-Control-Allow-Origin"]=origin;reply.hijack();reply.raw.writeHead(200,headers);reply.raw.write(`event: connected\ndata: ${JSON.stringify({at:new Date().toISOString()})}\n\n`);const unsubscribe=eventBus.subscribe(event=>reply.raw.write(`event: ${event.type}\ndata: ${JSON.stringify(event.payload)}\n\n`));const ping=setInterval(()=>reply.raw.write(": ping\n\n"),15000);request.raw.on("close",()=>{clearInterval(ping);unsubscribe();});});}
