import{ActivityIndicator,Pressable,StyleSheet,Text,type PressableProps}from"react-native";import{Ionicons}from"@expo/vector-icons";import{colors}from"@/constants/theme";
export function ActionButton({label,icon,variant="primary",busy=false,...props}:PressableProps&{label:string;icon:keyof typeof Ionicons.glyphMap;variant?:"primary"|"secondary"|"danger";busy?:boolean}){const background=variant==="primary"?colors.primary:variant==="danger"?colors.danger:colors.surface;const ink=variant==="secondary"?colors.ink:"#fff";return <Pressable accessibilityRole="button" style={({pressed})=>[styles.button,{backgroundColor:background,borderColor:variant==="secondary"?colors.line:background,opacity:pressed||props.disabled?.75:1}]} {...props}>{busy?<ActivityIndicator color={ink}/>:<><Ionicons name={icon} size={18} color={ink}/><Text style={[styles.label,{color:ink}]}>{label}</Text></>}</Pressable>}
const styles=StyleSheet.create({button:{minHeight:46,borderRadius:7,borderWidth:1,paddingHorizontal:16,flexDirection:"row",alignItems:"center",justifyContent:"center",gap:8},label:{fontSize:14,fontWeight:"700"}});
